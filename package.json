{
  "name": "ldash",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter ./server start",
    "test": "pnpm --filter ./server test"
  }
}
